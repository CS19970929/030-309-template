# Codex App 中使用 Git 分支与 Worktree 的实战说明

## 1. 这份文档解决什么问题

这份文档不是重复讲 Git 基础，而是专门回答下面这些更实际的问题：

- 在 Codex App 里怎么理解 `worktree`
- Codex App 自带的“隔离副本/并行 agent”与手动 `git worktree` 是什么关系
- 我当前正在改代码，又想让 Codex 去干另一件事，应该怎么操作
- 对当前这个固件项目，怎样组织“主线 / 重构线 / 热修线”最稳
- 给 Codex 的 prompt 应该怎么写，才能减少串线和误改

如果你还没把 Git 的基本概念吃透，建议先看：

[GIT_BRANCH_WORKTREE_GUIDE.md](/E:/TODO/030%20+%20309/GIT_BRANCH_WORKTREE_GUIDE.md)

---

## 2. 先说结论

在 Codex App 里使用 `worktree`，你可以分成两种模式理解：

### 模式 A：让 Codex App 在隔离副本里并行处理任务

特点：

- 你不需要自己先手动建 `git worktree`
- 适合快速并行开一个任务
- 更像“开一个独立 agent，让它在隔离环境里工作”

### 模式 B：你自己先手动创建 Git `worktree`，再用 Codex App 打开那个目录

特点：

- 目录、分支、职责边界都由你控制
- 最适合长期并行任务
- 最适合大重构
- 更适合当前这个项目

一句话：

- **想快速并行开工，用模式 A**
- **想长期、清晰、可控地并行开发，用模式 B**

---

## 3. Codex App 里的“隔离副本”怎么理解

Codex App 本身就支持多 agent 并行和隔离工作环境。  
这意味着你在 App 里开一个新任务时，不一定需要自己先去敲 `git worktree add`。

可以把它理解成：

- 你当前线程继续处理当前目录的工作
- 另一个 agent 在它自己的隔离副本里处理另一件事

这样做的好处是：

- 不会直接打断你当前工作
- 便于并行处理不同任务
- 审查 diff 更清楚

但要注意：

- 这是 Codex App 提供的工作隔离能力
- 它和你自己显式管理的 Git `worktree` 不是一回事
- 对短期任务很好用，对长期任务不如手动 `worktree` 直观

---

## 4. 什么时候直接用 Codex App 的隔离副本就够了

下面这些场景，用模式 A 就够：

- 你正在做一个任务，临时想让 Codex 并行查另一个问题
- 你想开一个短期 hotfix，不打断当前工作
- 你只关心“先把另一件事做出来”，暂时不关心目录怎么组织
- 任务本身比较短，不会持续很多天

### 4.1 实际操作思路

你只需要在 Codex App 里新开一个任务或 agent，然后明确告诉它：

- 这是独立任务
- 不要影响当前正在进行的改动
- 只处理指定目标

例如，热修型 prompt：

```text
请在独立 worktree 或隔离工作副本中处理这个任务，不要影响当前正在进行的改动。
目标：修复 xxx 问题，只做最小改动，不做重构。
要求：
1. 全部使用中文输出
2. 先分析影响范围，再修改
3. 修改完成后给出验证建议
4. 提交说明需要完整、可审阅
```

例如，分析型 prompt：

```text
请在独立 worktree 或隔离工作副本中处理这个任务，不要影响当前目录里的未完成改动。
目标：分析当前通信链路的模块边界和耦合点，给出重构方案，但先不要大范围改代码。
要求：
1. 全部使用中文输出
2. 先读代码再下结论
3. 识别风险点和可渐进改造路径
```

### 4.2 这种方式的优点

- 快
- 干净
- 不打断当前手头工作
- 很适合“边做边派活”

### 4.3 这种方式的局限

- 你对目录结构的掌控感没那么强
- 不适合长期维护一个明确的“重构目录”
- 当任务持续很多天时，不如手动 `worktree` 清晰

---

## 5. 什么时候应该自己手动创建 `worktree`

下面这些情况，我建议你用模式 B：

- 任务是长期的
- 你想把“重构线”和“维护线”物理分开
- 你需要明确知道哪个目录对应哪个分支
- 你会长期在两个目录之间切换
- 你要做大重构，并且希望提交和文档都干净

这正是当前这个项目最典型的需求。

---

## 6. 当前项目最适合的目录和职责划分

结合你现在这个仓库，我建议以后固定采用下面这种结构。

### 6.1 主目录

`E:\TODO\030 + 309`

用途：

- 当前维护线
- 小修小补
- 协议字段修正
- 紧急编译修复
- 兼容性验证

### 6.2 重构目录

`E:\TODO\030 + 309-refactor`

用途：

- 架构重构
- 模块边界梳理
- 通信层/协议层重新分层
- 工程目录整理
- 长周期改造

### 6.3 热修目录

`E:\TODO\030 + 309-hotfix`

用途：

- 突发问题
- 紧急客户问题
- 不想打断当前重构时的最小修复

---

## 7. 在 Codex App 里配合手动 `worktree` 的标准流程

这是最推荐你的工作方式。

### 7.1 第一步：在当前仓库终端创建 `worktree`

例如创建重构线：

```powershell
git worktree add -b codex/refactor-architecture "E:\TODO\030 + 309-refactor" HEAD
```

例如创建热修线：

```powershell
git worktree add -b codex/hotfix-urgent-issue "E:\TODO\030 + 309-hotfix" HEAD
```

### 7.2 第二步：在 Codex App 中打开新目录

创建完成后：

- 保留当前窗口继续看原目录
- 再打开一个窗口或新项目，指向新目录

例如：

- 一个窗口打开 `E:\TODO\030 + 309`
- 一个窗口打开 `E:\TODO\030 + 309-refactor`

### 7.3 第三步：给不同目录设定明确职责

例如：

- 原目录只做维护，不做大重构
- 重构目录只做架构调整，不顺手修无关 bug
- 热修目录只做最小修复，不趁机重构

### 7.4 第四步：给 Codex 的 prompt 里明确写边界

这是最关键的一步。  
不要只说“帮我改一下”，要明确告诉它：

- 当前目录是什么用途
- 不允许做什么
- 大改动是否需要文档
- 提交信息要求

---

## 8. 针对当前项目的推荐 prompt 模板

### 8.1 主线维护窗口

适合原目录 `E:\TODO\030 + 309`

```text
你当前在主维护线工作，只处理现有版本上的问题，不做整体重构。
请先阅读相关模块，分析最小修改范围，再进行修复。
要求：
1. 全部使用中文输出
2. 优先最小改动
3. 涉及协议映射、驱动时序、寄存器、内存布局时必须说明风险
4. 如果改动较大，补充文档并整理清晰的 git 提交说明
```

### 8.2 重构窗口

适合 `E:\TODO\030 + 309-refactor`

```text
你当前在独立重构 worktree 中工作，这里只处理架构重构，不处理主线上的临时修复。
请先阅读项目结构，识别通信层、协议层、驱动层、业务层的边界和耦合，再给出分阶段重构方案并逐步实施。
要求：
1. 全部使用中文输出
2. 非必要不改变现有功能行为
3. 大的代码变更必须生成配套文档
4. 完成后整理清晰、完整、可审阅的 git 提交说明
```

### 8.3 热修窗口

适合 `E:\TODO\030 + 309-hotfix`

```text
你当前在独立 hotfix worktree 中工作，这里只处理紧急修复，不做重构。
请先定位问题最相关的文件、调用链和影响范围，再按最小改动原则完成修复。
要求：
1. 全部使用中文输出
2. 优先保持现有稳定行为
3. 涉及协议、寄存器、时序或兼容逻辑时必须说明风险
4. 完成后给出验证建议和 git 提交说明
```

---

## 9. 一个最典型的使用场景

下面用你最容易遇到的情况举例。

### 场景

当前你在原目录里做通信层重构，改到一半，还没提交。  
这时又来了一个紧急问题：某个协议字段映射错误，必须立刻修。

### 错误做法

在同一个目录里：

1. 先 stash 当前重构
2. 切分支
3. 修 bug
4. 再切回来
5. 再恢复重构现场

这个流程不是不能用，而是：

- 容易乱
- 容易忘
- 容易把两个任务混在一起

### 更稳的做法

1. 保留原目录不动，继续保留重构现场
2. 新建一个 `hotfix` worktree
3. 在 Codex App 里打开 hotfix 目录
4. 在这个新目录中只处理紧急问题
5. 修完后单独提交和审查

这就是 `worktree` 在 Codex App 里最有价值的时刻。

---

## 10. 在 Codex App 中如何避免串线

多目录并行时，真正的问题通常不是 Git，而是人容易搞混。

建议你固定采用下面这些习惯：

### 10.1 目录名直接体现用途

例如：

- `030 + 309`
- `030 + 309-refactor`
- `030 + 309-hotfix`

### 10.2 分支名直接体现目标

例如：

- `codex/refactor-architecture`
- `codex/hotfix-protocol-mapping`
- `codex/cleanup-header-deps`

### 10.3 每个窗口只做一种事

不要在重构窗口顺手修热修问题。  
不要在热修窗口顺手做结构整理。

### 10.4 prompt 里明确禁止无关修改

例如：

```text
这里只处理热修，不做重构，不顺手整理无关代码。
```

### 10.5 大改动配文档

这点尤其适合你现在这个项目。

建议：

- 重构目录里的阶段性大改动都补文档
- 提交说明写清楚动机、范围、风险和验证方式

---

## 11. 什么时候不要执着于 `worktree`

虽然 `worktree` 很有用，但也不要为了用而用。

下面这些情况，没必要专门搞 `worktree`：

- 你只是想临时查一个小问题
- 只是一个 5 到 10 分钟的小修补
- 当前工作区本来就很干净
- 你不会长期并行维护两个任务

这种场景下：

- 直接在当前目录干
- 或者直接让 Codex App 开一个隔离副本做短任务

都可以。

---

## 12. 一个实用判断法

如果你不知道该不该用 `worktree`，直接问自己这三个问题：

### 12.1 这个新任务会持续很久吗

如果会，倾向于用 `worktree`。

### 12.2 我是否需要保留当前目录的未完成改动现场

如果需要，倾向于用 `worktree`。

### 12.3 我是否会在两个任务之间来回切很多次

如果会，倾向于用 `worktree`。

如果三个问题里有两个以上答案是“会”，基本就值得开。

---

## 13. 最后只记住这几句

如果只记最实用的内容，记住下面几句：

- Codex App 可以让任务在隔离副本里并行处理
- 短期并行任务，可以直接用 App 的隔离能力
- 长期并行任务，最好自己手动创建 Git `worktree`
- 大重构最适合“主目录 + 重构目录”的模式
- 热修最适合单独一个 `hotfix` worktree
- prompt 里必须写清楚当前目录职责和禁止事项
- 你的这个项目最适合把“维护、重构、热修”分到不同目录

---

## 14. 推荐你的实际工作流

结合当前项目，推荐采用下面这套固定模式：

### 日常模式

- 原目录：维护线
- 重构目录：长期重构
- 必要时临时再开一个热修目录

### 任务很短时

- 直接在 Codex App 里开一个隔离任务即可

### 任务很长时

- 手动建 `worktree`
- 再在 Codex App 中打开对应目录

这套方式最适合你后续既要重构，又要继续维护现有固件版本的工作节奏。
