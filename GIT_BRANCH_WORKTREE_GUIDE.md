# Git、分支与 Worktree 学习笔记

## 1. 这份文档解决什么问题

这份文档面向当前这个固件项目，目标不是讲一套抽象的 Git 教材，而是回答这些实际问题：

- Git 里到底有哪些核心概念，它们之间是什么关系
- 分支、工作区、暂存区、提交分别是什么
- `worktree` 到底是什么，为什么它不是“另一个仓库”
- 什么场景只开分支就够，什么场景适合加 `worktree`
- 如果正在改代码，突然想去做另一件事，应该怎么处理
- 在 Codex / 多任务 / 大重构场景下，怎样组织目录、分支和提交流程更稳

---

## 2. 先建立一个最重要的认知模型

可以先把 Git 记成四层：

1. 仓库历史：已经提交进 Git 的历史记录
2. 分支：指向某条历史线的“名字”
3. 工作区：你当前目录下真正看到和修改的文件
4. 暂存区：准备进入下一次提交的内容

一句话理解：

- **提交（commit）** 是一个历史快照
- **分支（branch）** 是指向提交的标签
- **工作区（working tree）** 是你正在编辑的文件现场
- **暂存区（index / staging area）** 是“准备提交哪些改动”的中间层

这四层里，最容易混淆的是：

- 分支不是文件夹
- 工作区不是分支
- `worktree` 不是新仓库

---

## 3. Git 的最基本对象

### 3.1 提交 `commit`

每次提交都是一个“当时项目状态的快照”，并且会记录：

- 提交说明
- 作者
- 时间
- 父提交

可以把它理解成项目的一个存档点。

### 3.2 分支 `branch`

分支本质上只是一个指针，指向某个提交。

例如：

- `main`
- `develop`
- `codex/refactor-architecture`

当你在某个分支上继续提交时，这个分支的指针会不断向前移动。

所以分支的作用不是“复制一份代码”，而是：

- 给一条开发线命名
- 让这条历史线可以独立演化
- 以后再决定是否合并回其他分支

### 3.3 `HEAD`

`HEAD` 表示“你当前所在的位置”。

通常情况下，`HEAD` 会指向当前分支；当前分支再指向某个提交。

例如：

- `HEAD -> codex/legacy-board-compat -> d4f60c3`

### 3.4 工作区 `working tree`

就是你当前目录里的真实文件。

你打开编辑器看到的 `.c`、`.h`、`.uvprojx` 都属于工作区内容。

你改文件时，最先变化的是工作区，而不是 Git 历史。

### 3.5 暂存区 `staging area`

暂存区是“准备进入下一次提交”的缓冲层。

例如：

```powershell
git add Code/Source/Comm.c
```

这时不是“已经提交”，只是把这部分修改放进暂存区，等下一次 `git commit` 才真正进入历史。

---

## 4. 日常操作到底发生了什么

下面这段是最核心的日常流程：

1. 你修改文件
2. 改动先出现在工作区
3. `git add` 后，改动进入暂存区
4. `git commit` 后，暂存区内容形成新提交

一个很常见的误区是：

- 改了文件，不等于提交了
- `git add` 了，不等于提交了
- 切分支时真正容易冲突的是“工作区未提交改动”

---

## 5. 分支到底解决什么问题

分支用于把不同开发目标隔离开。

最典型的例子：

- `main` 保持稳定
- `codex/bugfix-uart-timeout` 修一个问题
- `codex/refactor-architecture` 做长期重构

适合开分支的场景：

- 新功能开发
- bug 修复
- 长周期重构
- 实验性改造
- 为 PR 准备独立提交历史

只要你想让“这批提交和主线暂时分开”，就应该开分支。

### 5.1 分支不是目录

很多人会潜意识觉得“我开了个分支，就像开了个副本目录”。

这不对。

分支只是 Git 历史中的一条线，默认情况下你还是在同一个本地目录里工作。  
你在这个目录里切换分支时，Git 会尝试把文件切换成目标分支对应的样子。

这就引出 `worktree` 的价值。

---

## 6. `worktree` 到底是什么

### 6.1 一句话定义

`worktree` 是：

**同一个 Git 仓库的额外工作目录。**

也可以换一种更直白的说法：

**让同一个仓库的不同分支，可以同时出现在不同本地目录里。**

### 6.2 它不是什么

`worktree` 不是：

- 新仓库
- 仓库完整复制
- 替代分支的东西

它依然属于同一个 Git 仓库，只是给这个仓库多挂了一个工作目录。

### 6.3 最直观的理解

假设当前只有一个目录：

- `E:\TODO\030 + 309`

你在里面做重构。  
这时又来了个紧急 bug，要去另一个分支修。

如果不用 `worktree`，你通常要：

1. 处理当前未提交改动
2. 切分支
3. 修 bug
4. 再切回重构分支
5. 恢复之前状态

如果用了 `worktree`，你可以得到两个目录：

- `E:\TODO\030 + 309`：继续做重构
- `E:\TODO\030 + 309-hotfix`：修紧急 bug

这两个目录可以同时打开、同时工作、互不打扰。

### 6.4 本质区别

- **分支**：解决“历史上分几条线”
- **worktree**：解决“本地能不能同时开多个工作目录”

---

## 7. 正在改代码时，新开 worktree，文件状态会怎样

这是最容易问到的点。

假设当前目录有未提交改动：

- 改了 `Code/Source/Comm.c`
- 改了 `Code/Source/conf/conf.h`
- 还有几个新文件没提交

这时再开一个新的 `worktree`：

```powershell
git worktree add ../project-hotfix main
```

那么文件状态是：

### 7.1 原目录

原目录里的未提交改动会完整保留：

- `modified` 还在
- `untracked` 还在
- 你改到一半的内容还在

### 7.2 新目录

新目录会是一个独立工作区，通常是干净的：

- 它只拿到你指定分支或提交点上的文件状态
- 看不到原目录里那些未提交改动
- 一般 `git status` 是干净的

### 7.3 关键结论

**未提交改动不会自动跟着你进入新 worktree。**

这正是 `worktree` 最重要的价值之一：  
它隔离的是“工作现场”。

---

## 8. `worktree` 最适合哪些场景

### 8.1 大重构和主线维护并行

这对当前项目最有价值。

例如：

- 原目录继续做兼容修复、验证、小改动
- 新目录专门做架构重构

适合原因：

- 重构周期长
- 主线还得继续跑
- 不想反复 stash / checkout

### 8.2 正在做一个任务，突然插进来紧急问题

例如你正在改通信层，突然要修一个兼容 bug。

这时最稳的方式不是打断当前目录，而是：

- 保留当前目录的改动现场
- 新开一个 `worktree` 专门处理 hotfix

### 8.3 两个任务都要持续几天以上

如果任务只是 10 分钟的小修补，直接切分支也行。  
但如果两个任务都要持续很久，`worktree` 会明显省心。

### 8.4 需要同时参考旧实现和新实现

例如重写通信栈、协议适配层或模块结构时：

- 一个目录看旧代码
- 一个目录写新代码

比在同一个目录来回切分支直观很多。

### 8.5 同时准备多个 PR

例如：

- PR1 修协议字段映射
- PR2 做工程清理
- PR3 做架构重构

每个任务一个独立目录，最不容易混改。

---

## 9. 什么情况下没必要用 `worktree`

下面这些情况，普通分支就够了：

- 你手头只有一个任务
- 切分支频率不高
- 当前工作区很干净
- 改动量很小
- 你不介意 `checkout`
- 任务之间不会互相打断

一句话：

**不是所有分支都要配 `worktree`。**

只有当“并行开发多个任务”已经开始带来工作区切换痛苦时，`worktree` 才真正有价值。

---

## 10. 当前项目最适合的使用方式

结合当前这个仓库，我建议这样理解和使用。

### 10.1 只做一个长期任务时

如果你接下来很长一段时间只做一件事，比如“整体架构重构”，那么：

- 开一个新分支即可
- 不一定需要 `worktree`

这种场景下，`worktree` 不是必须。

### 10.2 重构期间还要继续维护现有版本

这是最适合 `worktree` 的模式。

建议结构：

- 原目录：当前维护线
- `-refactor` 目录：长期重构线
- `-hotfix` 目录：临时修复线

例如：

- `E:\TODO\030 + 309`
- `E:\TODO\030 + 309-refactor`
- `E:\TODO\030 + 309-hotfix`

### 10.3 这个项目为什么特别适合

这个仓库是嵌入式固件项目，特点是：

- 通信层、协议层、配置层、驱动层容易相互牵连
- 历史兼容性问题比较多
- 一次修改可能影响编译、内存、协议映射、行为时序
- 大重构往往不是一天能结束

所以一旦进入“重构 + 维护并行”的状态，`worktree` 会比纯分支切换稳很多。

---

## 11. Git 常见概念对照表

| 概念 | 本质 | 你最该怎么理解 |
| --- | --- | --- |
| repository | 仓库 | 整个 Git 项目及其历史 |
| commit | 提交 | 一个历史快照 |
| branch | 分支 | 指向某条历史线的名字 |
| HEAD | 当前指向 | 你现在所在位置 |
| working tree | 工作区 | 当前目录里的真实文件 |
| staging area | 暂存区 | 准备进入下一次提交的内容 |
| stash | 临时收纳 | 临时把未提交改动收起来 |
| merge | 合并 | 把一条历史线并进另一条 |
| rebase | 变基 | 重写提交基线，让历史更线性 |
| worktree | 额外工作目录 | 同一仓库的另一个本地工作现场 |

---

## 12. stash、分支、worktree 三者如何取舍

这是实战里最常见的选择题。

### 12.1 什么时候用 `stash`

适合：

- 只是临时切出去处理一个很短的事情
- 当前改动还不想提交
- 很快就会回来

不适合：

- 任务很多且频繁切换
- 长期并行开发
- 容易忘记 stash 里到底塞了什么

### 12.2 什么时候只开分支

适合：

- 新任务本身需要独立历史
- 但你不会长期并行操作两个任务

### 12.3 什么时候用 `worktree`

适合：

- 已经确定要并行做两件或更多事情
- 不想让一个目录来回切分支
- 希望每个任务有独立工作目录

### 12.4 一个简单判断法

如果只是“临时切一下”：

- 先考虑 `stash`

如果是“要单独走一条开发线”：

- 先开分支

如果是“要长期并行做多条开发线”：

- 分支 + `worktree`

---

## 13. reset、restore、revert 的区别

这三个命令非常容易混。

### 13.1 `git restore`

主要用于恢复工作区或暂存区内容。

例如：

- 丢弃某个文件的工作区修改
- 把暂存区内容恢复掉

它更像“恢复文件状态”。

### 13.2 `git reset`

主要用于移动当前分支指针，或调整暂存区。

常见用途：

- 撤销 `git add`
- 把分支回退到某个提交

注意：

- `--hard` 风险很高，会改工作区
- 在多人协作或已有未备份改动时要非常谨慎

### 13.3 `git revert`

它不会删历史，而是“再做一个新提交，把之前某次提交的影响反向抵消掉”。

适合：

- 已经推送过的提交
- 不想破坏已有历史
- 需要保留审计链

一句话：

- 恢复文件，多想 `restore`
- 调整历史，多想 `reset`
- 不改老历史、追加一个反向提交，多想 `revert`

---

## 14. merge 和 rebase 怎么理解

### 14.1 `merge`

把一条分支的结果合并进另一条分支。

优点：

- 安全、直观
- 不会改已有提交历史

缺点：

- 历史图可能更复杂

### 14.2 `rebase`

把当前分支的提交“挪到”另一条分支后面，看起来像从新的基线开始开发。

优点：

- 历史更线性
- 阅读提交更顺

风险：

- 会重写提交历史
- 已经共享给别人的分支上要谨慎

对你当前这个项目，保守建议是：

- 本地整理历史可以用 `rebase`
- 已经共享出去的分支，优先用 `merge`

---

## 15. 常用命令与直观含义

### 15.1 看状态

```powershell
git status
```

查看当前分支、工作区改动、暂存区改动。

### 15.2 新建并切换分支

```powershell
git checkout -b codex/refactor-architecture
```

或：

```powershell
git switch -c codex/refactor-architecture
```

### 15.3 查看分支

```powershell
git branch
```

### 15.4 暂存文件

```powershell
git add Code/Source/Comm.c
```

### 15.5 提交

```powershell
git commit -m "Refactor comm transport and isolate protocol service"
```

### 15.6 临时收起未提交改动

```powershell
git stash push -u -m "before hotfix"
```

### 15.7 查看 `worktree`

```powershell
git worktree list
```

### 15.8 新建 `worktree`

```powershell
git worktree add -b codex/refactor-architecture ..\030-309-refactor HEAD
```

含义：

- 新建分支 `codex/refactor-architecture`
- 基于当前 `HEAD`
- 在 `..\030-309-refactor` 创建一个新工作目录

### 15.9 删除不再需要的 `worktree`

```powershell
git worktree remove ..\030-309-refactor
```

如果目录已手动删除，可能还需要：

```powershell
git worktree prune
```

---

## 16. 给当前项目的实战建议

### 16.1 分支命名建议

建议继续使用现在这种清晰命名方式：

- `codex/legacy-board-compat`
- `codex/refactor-architecture`
- `codex/hotfix-protocol-mapping`
- `codex/cleanup-header-deps`

命名原则：

- 前缀统一
- 一眼看出任务目标
- 不要用模糊名字，如 `test1`、`newfix`、`temp`

### 16.2 提交粒度建议

对固件项目尤其重要：

- 一次提交只做一类事情
- 协议修复、工程清理、架构调整尽量分开
- 大改动要配套文档

不推荐：

- 一个提交里同时改通信协议、内存布局、驱动初始化和注释

推荐：

1. 先做一个“抽接口”的提交
2. 再做一个“迁移实现”的提交
3. 再做一个“清理旧路径”的提交
4. 文档单独提交或与对应大改一起提交

### 16.3 大重构建议流程

1. 从稳定点开新分支
2. 如果主线还要继续维护，再配一个 `worktree`
3. 先写一份重构文档，明确模块边界和阶段目标
4. 按阶段提交，不要一口气把所有东西搅在一起
5. 定期同步主线变更
6. 最后再统一做合并或整合

---

## 17. 针对你最常见问题的直接回答

### 17.1 “我要完全重构项目架构，是开分支还是开 `worktree`？”

先开分支。  
如果重构期间还要继续维护当前版本，再加 `worktree`。

### 17.2 “我正在改代码，突然想去做别的事，要不要开 `worktree`？”

如果这个“别的事”只是几分钟的小事，不一定要。  
如果它会形成一条独立任务线，或者你不想打断当前改动，适合开 `worktree`。

### 17.3 “开了 `worktree` 后，新目录里会有我当前未提交改动吗？”

不会。  
当前目录的未提交改动保留在当前目录；新目录默认只拿到指定分支或提交点的状态。

### 17.4 “`worktree` 和复制一份项目目录有什么区别？”

复制目录会复制出一个新的普通文件夹，甚至可能变成新的仓库副本。  
`worktree` 是 Git 官方支持的同仓库多工作目录机制，更规范，也更清楚。

### 17.5 “同一个分支能不能同时开在两个 `worktree` 里？”

一般不能把同一个本地分支同时 checkout 到两个 `worktree`。  
通常做法是一个目录对应一个分支。

---

## 18. 适合交给 Codex 的 prompt 模板

下面这些模板适合你后续在不同分支或 `worktree` 里直接复用。

### 18.1 重构型

```text
你现在在一个独立分支或 worktree 中工作，这里只处理架构重构，不处理主线上的临时修复。
请先阅读项目结构，识别通信层、协议层、驱动层、业务层之间的边界和耦合，再给出分阶段重构方案并逐步实施。
要求：
1. 全部使用中文输出
2. 优先控制风险，避免无关修改
3. 大的代码变更必须补充文档
4. 完成后整理清晰、可审阅的 git 提交说明
5. 如果行为可能变化，必须明确指出影响范围和验证建议
```

### 18.2 热修型

```text
你现在在一个独立分支或 worktree 中处理紧急修复，不做重构。
请先定位问题最相关的文件、调用链和影响范围，再以最小改动原则完成修复。
要求：
1. 全部使用中文输出
2. 优先保证现有行为稳定
3. 涉及协议映射、寄存器、时序或内存布局时，必须说明风险
4. 修改完成后给出验证建议和 git 提交说明
```

### 18.3 清理整理型

```text
你现在在一个独立分支或 worktree 中做工程清理，不新增业务功能。
请先识别头文件依赖、重复定义、明显耦合、构建噪音和可维护性问题，再按低风险优先级逐步整理。
要求：
1. 全部使用中文输出
2. 不随意改变业务逻辑
3. 大改动补充文档
4. 最终给出清晰的 git 提交说明和验证建议
```

---

## 19. 最后只记住这几句

如果只想记最核心的内容，记住下面几句就够了：

- 分支是历史线，不是目录
- 工作区是你当前真正改文件的地方
- `git add` 是进暂存区，不是提交
- `worktree` 是同一个仓库的额外工作目录
- 只做一条任务线时，开分支通常就够了
- 多条任务线长期并行时，分支加 `worktree` 最省心
- 新开的 `worktree` 不会自动带上当前目录里的未提交改动

---

## 20. 建议你的后续学习顺序

如果你后面准备认真补 Git，建议按这个顺序看：

1. 先彻底理解工作区、暂存区、提交
2. 再理解分支和 `HEAD`
3. 再理解 `stash`
4. 再理解 `merge` 和 `rebase`
5. 最后把 `worktree` 当成“多工作目录工具”来看

这个顺序比一开始就硬啃命令表更容易真正搞明白。
