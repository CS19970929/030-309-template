# App 架构重构说明 2026-03-18

## 背景

当前应用层启动流程主要集中在 [main.c](/E:/TODO/030%20+%20309/Code/Source/main.c) 中，原实现存在几个问题：

- `main()` 同时承担启动、任务注册、前台轮询和条件编译分支逻辑，职责过重
- Debug / Release 初始化流程有较多重复代码
- 周期任务注册散落在启动代码中，不利于后续增减任务或审查启动行为
- 应用层入口不够清晰，阅读时不容易快速判断“平台初始化”“运行态初始化”“前台服务循环”分别在哪里

这次重构的目标不是改变功能，而是降低应用层编排复杂度，给后续继续重构业务模块打基础。

## 目标

本次重构明确保持以下内容不变：

- 外部通信接口不变
- 通信地址不变
- 任务执行周期不变
- 启动行为顺序不变
- 现有对外函数接口不变

本次只优化应用层编排结构，优先保证稳定性和可读性。

## 本次改动

重构集中在 [main.c](/E:/TODO/030%20+%20309/Code/Source/main.c)，主要做了四件事：

1. 引入应用层生命周期划分

- `App_InitPlatform()`
- `App_InitRuntimeState()`
- `App_RegisterScheduledTasks()`
- `App_RunForegroundServices()`
- `App_RunMainLoop()`

这样 `main()` 只保留最高层入口职责。

2. 把周期任务注册改为表驱动

通过 `AppTaskConfig` 把周期任务集中声明，避免启动流程里一长串 `SCH_Add_Task(...)` 分散出现。

3. 抽出预充启动逻辑

将预充相关逻辑收敛到 `App_ApplyPrechargeStartup()`，避免设备初始化流程继续膨胀。

4. 保持 Debug / Release 行为边界清晰

把只有 Release 使用的前台服务和周期任务继续保留在条件编译分支内，但不再让它们直接污染 `main()` 主流程。

## 效果

这次改动后的应用层结构可以理解为：

1. 平台初始化
2. 通信运行态初始化
3. 周期任务注册
4. 进入主循环
5. 主循环中统一执行调度器和前台服务

这样做的收益：

- `main()` 入口更简洁
- 启动代码职责边界更清楚
- 后续要增加、删减、审查周期任务更容易
- 更适合后续继续把业务模块从“编排逻辑”中解耦

## 风险控制

为了控制风险，这次特意没有做下面这些事情：

- 没改通信协议实现
- 没改寄存器映射
- 没改地址定义
- 没改调度周期
- 没改业务模块函数签名
- 没改 `Comm.c`、`Sci_Upper.c` 等通信链路实现

也就是说，这次是“应用层编排重构”，不是“业务逻辑重写”。

## 未继续下钻的部分

原本还计划把 [DataDeal.c](/E:/TODO/030%20+%20309/Code/Source/DataDeal.c) 中 `App_AFEGet()` 的采样编排继续收敛一层，但该文件当前不是 UTF-8 编码，现有 patch 工具无法安全编辑。

为了避免编码破坏，本次先不动它，后续如果要继续深入，建议先统一相关源文件编码，再做第二轮重构。

## 验证情况

当前环境未找到可直接执行的 ARM GCC 工具链，也未检测到可直接命令行调用的 Keil `UV4`，因此这次没有完成本地编译验证。

本次实际完成的是：

- 代码结构审查
- 改动范围收敛
- 外部接口与行为路径约束

后续建议补做的验证：

1. Keil 全量编译
2. 上电启动验证
3. 通信读写回归验证
4. 预充与休眠相关启动路径验证

## 后续建议

如果这条分支要继续做更大范围的 App 架构优化，建议按下面顺序推进：

1. 先统一关键源文件编码
2. 再收敛 `App_AFEGet()`、`App_ProID_Deal()` 等应用编排函数
3. 再进一步拆分“平台初始化”和“业务初始化”
4. 最后才考虑对业务模块本身做更深层解耦

这样比一次性全面下刀更稳。
