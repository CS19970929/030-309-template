# 当前项目的 Git / 分支 / Worktree 开发规范

## 1. 文档目标

这份文档不是讲 Git 教程，而是结合当前项目的结构、风险点和你的使用习惯，整理出一套可以长期执行的开发规范。

目标有四个：

- 让你在这个 STM32 / Keil 固件项目里更稳地使用 Git
- 让分支、`worktree`、文档、提交形成固定工作流
- 降低“大改动混在一起”“改到一半被打断”“提交历史难审阅”的问题
- 把你的开发习惯逐步规范成可复用、可审查、可回滚的方式

如果要先补基础概念，建议配合阅读：

- [GIT_BRANCH_WORKTREE_GUIDE.md](/E:/TODO/030%20+%20309/GIT_BRANCH_WORKTREE_GUIDE.md)
- [CODEX_APP_WORKTREE_PRACTICE.md](/E:/TODO/030%20+%20309/CODEX_APP_WORKTREE_PRACTICE.md)

---

## 2. 对当前项目的判断

结合当前仓库，可以明确看出这是一个典型的嵌入式固件工程，具备以下特点：

- 使用 STM32 固件工程结构，核心代码在 `Code/Source`、`Code/Drivers`
- 使用 Keil 工程文件，存在 `.uvprojx`、`.uvoptx`、`.uvguix.*`
- 构建和调试过程会产生 `.map`、`.lst`、`Objects/`、`DebugConfig/` 等衍生文件
- 通信层、协议层、驱动层、配置层之间耦合较高
- 存在历史兼容性需求，例如旧板兼容、协议适配、字段映射、唤醒逻辑等
- 大改动通常不只是“改代码”，还需要同步说明行为、风险和验证方式

这意味着你这个项目最怕的不是“不会写命令”，而是下面这些问题：

- 一个提交里混入多个目标，后面难以复盘
- 重构、热修、兼容适配混在同一条线里
- 改到一半被打断，不得不 stash / 切分支 / 再切回来
- 把环境文件、构建产物、临时文件混进提交
- 大改动没有文档，过几天自己都说不清为什么这么改

所以，这个项目更需要的是**流程纪律**，不是命令技巧。

---

## 3. 总体原则

后续开发建议长期坚持下面这些原则。

### 3.1 一个目录只承担一种职责

不要让一个目录同时承载：

- 主线维护
- 长期重构
- 临时热修

这是最容易把工作区搞乱的方式。

### 3.2 一个分支只承载一个目标

不要在同一个分支里同时做：

- 协议字段修复
- 架构重构
- 构建清理
- 驱动行为调整

如果必须一起做，说明任务边界一开始就没切清楚。

### 3.3 一次提交只表达一件事

提交要能被人读懂。  
理想状态是看到一条提交说明，就能大致知道：

- 为什么改
- 改了什么
- 风险在哪

### 3.4 大改动必须配文档

这条要当成硬规则，而不是“有空再补”。

对当前项目，以下类型都应视为大改动：

- 模块重构
- 协议适配重写
- 驱动逻辑调整
- 唤醒 / 休眠 / 存储流程修改
- 内存布局或 map 优化
- 兼容性逻辑调整

### 3.5 主动隔离风险

对这个项目来说，最有效的风险控制方式不是“改完再看”，而是：

- 用分支隔离目标
- 用 `worktree` 隔离现场
- 用文档隔离认知
- 用小提交隔离回滚点

---

## 4. 推荐的目录与分支组织方式

### 4.1 默认目录角色

建议固定采用以下约定。

主目录：

`E:\TODO\030 + 309`

职责：

- 当前维护线
- 已知问题修复
- 小规模兼容调整
- 编译验证

重构目录：

`E:\TODO\030 + 309-refactor`

职责：

- 架构重构
- 模块拆分
- 通信层、协议层分层
- 长周期改造

热修目录：

`E:\TODO\030 + 309-hotfix`

职责：

- 突发问题
- 紧急兼容处理
- 不影响当前重构的最小修复

### 4.2 推荐分支命名

建议统一以 `codex/` 为前缀，并保持目标可读。

推荐示例：

- `codex/refactor-architecture`
- `codex/hotfix-protocol-mapping`
- `codex/legacy-board-compat`
- `codex/cleanup-header-deps`
- `codex/reduce-map-size`

不推荐：

- `fix1`
- `test`
- `new`
- `tmp`

命名标准：

- 前缀统一
- 目标单一
- 读名字就知道用途

---

## 5. 任务开始前的标准动作

以后开始任何任务，建议固定先做这几步。

### 5.1 先判断任务类型

把任务分成四类：

1. 热修
2. 小功能或小改动
3. 中等规模整理
4. 大重构

判断完成后再决定：

- 只开分支
- 分支加 `worktree`
- 是否必须先写文档

### 5.2 先看当前仓库状态

```powershell
git status
git branch --show-current
git worktree list
```

这一步的目的是避免：

- 忘了自己在哪个分支
- 当前目录里有脏改动
- 已经存在相关 `worktree` 却又重复开

### 5.3 先确认这次改动的边界

开工前要先能说清楚：

- 这次任务只改哪些模块
- 哪些模块明确不碰
- 这次是否允许行为变化
- 是否需要回归验证

如果这四个问题说不清，通常说明任务边界还不够清楚，不适合直接大改。

---

## 6. 什么时候只开分支，什么时候加 `worktree`

### 6.1 只开分支即可

适合：

- 当前只有一个主要任务
- 不需要长期并行做另一件事
- 当前目录是干净的
- 改动规模可控

典型例子：

- 修一个明确的协议字段错位
- 小范围编译修复
- 调整某个单独模块的常量或判断逻辑

### 6.2 分支加 `worktree`

适合：

- 正在做长期任务，又临时来了另一件事
- 架构重构期间仍要维护当前版本
- 同时开两个以上任务
- 不想频繁 stash / checkout
- 想把不同任务彻底物理隔离

对你来说，最常见的两个场景是：

- 一边做通信/协议重构，一边修主线兼容问题
- 一边做 map / 内存 / 结构清理，一边处理紧急协议 bug

### 6.3 一个简单判断法

满足下面任意两个条件，就优先考虑 `worktree`：

- 任务持续时间超过半天
- 当前目录已经有未提交改动
- 你预计会在两个任务间切换多次
- 你希望两个任务分别形成干净提交历史

---

## 7. 针对这个项目的推荐工作流

下面是我建议你长期固定使用的工作流。

### 7.1 热修工作流

适合：

- 编译错误
- 协议字段映射错误
- 明确的兼容 bug
- 紧急客户问题

流程：

1. 从当前稳定点开 `hotfix` 分支
2. 如果当前目录已有进行中的工作，直接新建 `hotfix worktree`
3. 只做最小必要修改
4. 必要时补一份问题说明文档
5. 单独提交

要求：

- 不顺手重构
- 不顺手整理无关代码
- 不把临时调试痕迹一起提交

### 7.2 小改动工作流

适合：

- 单模块逻辑修正
- 参数调整
- 注释和可读性优化
- 轻量构建整理

流程：

1. 开独立分支
2. 明确本次只碰哪些文件
3. 完成修改
4. 编译或最小验证
5. 提交

### 7.3 大重构工作流

适合：

- 通信层重构
- 协议适配层重写
- 目录重组
- 业务层与驱动层解耦
- 大范围 map / 内存优化

流程：

1. 开独立重构分支
2. 如果主线还要继续维护，再开独立 `worktree`
3. 先写重构文档
4. 分阶段实施
5. 每一阶段都形成清晰提交
6. 重要节点同步文档
7. 最后再做汇总提交或整合

这类任务严禁一上来就“直接全改完再说”。

---

## 8. 文档规则

根据你的要求，后续建议固定采用下面这套规则。

### 8.1 什么时候必须写文档

以下情况必须写文档：

- 模块结构变化
- 核心流程变化
- 接口边界变化
- 协议映射规则调整
- 驱动行为逻辑变化
- 存储、唤醒、休眠、安全相关逻辑变化
- map / RAM / Flash 占用优化

### 8.2 文档建议命名

继续沿用你仓库现在已有的风格：

- `COMM_PROTOCOL_REFACTOR.md`
- `STORAGE_IAP_REVIEW.md`
- `XXX_REVIEW.md`
- `XXX_SUMMARY.md`
- `XXX_YYYYMMDD.md`

建议规则：

- 与任务目标直接对应
- 一眼看出主题
- 必要时带日期

### 8.3 文档最少应包含什么

对于项目内文档，建议最少包含这几段：

1. 背景
2. 问题
3. 修改思路
4. 影响范围
5. 风险点
6. 验证方式

如果是重构文档，再加：

7. 阶段拆分
8. 后续事项

---

## 9. 提交规范

### 9.1 当前项目的提交目标

好的提交信息应该能回答三个问题：

- 为什么改
- 改了什么
- 主要影响是什么

### 9.2 推荐提交风格

结合你现在的习惯，我建议继续保持“简洁但可读”的风格，可以是中文，也可以是中英结合，但要避免模糊。

推荐示例：

- `修正 ASCII 从机 0x61 字段错位与位置编码`
- `Refactor comm transport and isolate protocol service`
- `Reduce protocol path size and restore successful link`
- `Switch balance control to SH367309 internal threshold`

### 9.3 不推荐的提交信息

- `fix`
- `update`
- `改一下`
- `测试`
- `提交`

### 9.4 大改动的提交建议

对大改动，不建议只用一条笼统提交把所有变化吞进去。

建议拆成：

1. 文档/方案提交
2. 基础结构调整提交
3. 具体行为迁移提交
4. 清理旧路径提交

如果最终还需要一个总结性提交，也可以加，但不应该只有它。

---

## 10. 文件提交范围规范

这个项目有很多“容易混进仓库但并不应该成为逻辑提交主体”的文件类型。

### 10.1 应优先关注的文件

通常应作为主要提交内容的：

- `Code/Source/**/*.c`
- `Code/Source/**/*.h`
- `Code/Drivers/**/*.c`
- `Code/Drivers/**/*.h`
- 项目必要配置文件
- 评审/说明/重构文档

### 10.2 需要谨慎对待的文件

以下文件经常会变化，但不一定应该每次都提交：

- `.uvoptx`
- `.uvguix.*`
- `.map`
- `.lst`
- `Objects/`
- `DebugConfig/`
- 调试日志
- 本地 IDE 状态文件

原则：

- 如果它们只是本地环境副产物，不要混进功能提交
- 如果你这次任务就是分析 map、定位链接问题或需要保留构建证据，再单独说明为什么提交它们

### 10.3 关于 `.gitignore`

后续建议继续逐步完善 `.gitignore`，减少环境噪音。  
但在没完全整理好之前，更要靠提交前人工检查，避免把无关文件一起提交。

---

## 11. 提交前检查清单

以后每次提交前，建议固定过一遍下面这张清单。

### 11.1 范围检查

- 当前分支是否正确
- 当前目录职责是否正确
- 是否混入了不属于本任务的文件

### 11.2 逻辑检查

- 是否顺手改了无关逻辑
- 是否引入了行为变化但没说明
- 是否存在调试代码、临时宏、临时日志没清理

### 11.3 文档检查

- 这次改动是否达到“必须写文档”的标准
- 文档是否写了背景、风险和验证方式

### 11.4 提交信息检查

- 看到提交说明，别人能否理解目的
- 提交说明是否过于笼统

### 11.5 构建检查

- 是否至少做了最小编译验证
- 如未验证，是否明确写出来

---

## 12. 针对 Codex 的推荐使用方法

你已经在用 Codex 辅助开发，后续建议固定用法，不要每次都临场发挥。

### 12.1 先告诉 Codex 当前目录角色

例如：

```text
你当前在主维护线工作，只处理现有版本上的问题，不做整体重构。
```

或者：

```text
你当前在独立重构 worktree 中工作，这里只处理架构重构，不处理主线上的临时修复。
```

### 12.2 再告诉 Codex 改动边界

例如：

```text
本次只允许修改通信层和协议适配相关模块，不要顺手整理其他目录。
```

### 12.3 明确文档和提交要求

例如：

```text
如果改动较大，必须补充文档；完成后整理清晰、完整、可审阅的 git 提交说明。
```

### 12.4 明确风险敏感点

这个项目里最需要明确提醒的点包括：

- 协议字段映射
- 时序
- 寄存器行为
- 内存布局
- 兼容逻辑
- 唤醒 / 休眠 / 存储流程

例如：

```text
涉及协议映射、寄存器、时序或内存布局时，必须明确说明风险和验证建议。
```

---

## 13. 推荐的固定 Prompt 模板

### 13.1 维护线模板

```text
你当前在主维护线工作，只处理现有版本上的问题，不做整体重构。
请先阅读相关模块，分析最小修改范围，再进行修复。
要求：
1. 全部使用中文输出
2. 优先最小改动
3. 涉及协议映射、寄存器、时序、内存布局或兼容逻辑时，必须说明风险
4. 如果改动较大，补充文档并整理清晰的 git 提交说明
```

### 13.2 重构线模板

```text
你当前在独立重构分支或 worktree 中工作，这里只处理架构重构，不处理主线上的临时修复。
请先阅读项目结构，识别通信层、协议层、驱动层、业务层之间的边界和耦合，再给出分阶段方案并逐步实施。
要求：
1. 全部使用中文输出
2. 非必要不改变现有功能行为
3. 大的代码变更必须配套文档
4. 每个阶段结束后整理清晰、可审阅的 git 提交说明
5. 涉及内存布局、协议映射、状态机或时序变化时必须明确说明风险
```

### 13.3 热修线模板

```text
你当前在独立 hotfix 分支或 worktree 中工作，这里只处理紧急修复，不做重构。
请先定位问题最相关的文件、调用链和影响范围，再按最小改动原则完成修复。
要求：
1. 全部使用中文输出
2. 不顺手整理无关代码
3. 保持现有行为稳定优先
4. 完成后给出验证建议和清晰的 git 提交说明
```

---

## 14. 你最需要改进的开发习惯

这部分是规范，也是建议。

### 14.1 不要让“当前目录”同时承担多个任务

这是后续最值得长期改掉的习惯。  
一旦发现当前目录已经在做 A，但又想开始 B，就先判断要不要开新分支或 `worktree`。

### 14.2 不要把“顺手改一点”当成正常行为

在固件项目里，“顺手改一点”常常是风险来源。

顺手改动最容易导致：

- 本次提交边界不清
- 回归问题难定位
- 以后审查时看不懂改动动机

### 14.3 先写清任务边界，再动代码

尤其是重构。  
先用几句话写清楚：

- 为什么改
- 只改什么
- 不改什么

这一步非常值。

### 14.4 大改动先写文档，不要等改完补

改完再补文档，往往已经忘了很多关键取舍。  
重构类任务建议一开始就先写背景和目标，再逐步补充实施细节。

### 14.5 提交前主动剔除环境噪音

像 `.uvoptx`、`.uvguix.*`、`.map`、`.lst` 这类文件，后续建议你养成固定检查习惯。

不是说永远不能提交，而是：

- 默认不提交
- 需要提交时，说明原因

---

## 15. 一套建议你长期坚持的标准节奏

后续每次开发，尽量按下面这个节奏来。

### 阶段 1：定义任务

- 这是热修、普通改动还是重构
- 需要什么分支
- 是否需要 `worktree`
- 是否需要先写文档

### 阶段 2：建立隔离

- 切到正确分支
- 必要时新建 `worktree`
- 明确当前目录职责

### 阶段 3：实施修改

- 控制改动边界
- 保持单一目标
- 避免混入环境文件和无关改动

### 阶段 4：整理说明

- 更新或补充文档
- 整理提交说明
- 记录风险点和验证方法

### 阶段 5：提交

- 检查文件范围
- 检查构建状态
- 检查提交信息可读性
- 再提交

---

## 16. 最后只记住这些规则

如果你后面不想每次都重看全文，只记住下面这些：

- 一个目录只做一类事
- 一个分支只承载一个目标
- 一个提交只表达一件事
- 大改动必须配文档
- 重构期间还要维护主线时，用 `worktree`
- 提交前先剔除环境噪音文件
- prompt 里必须写清楚目录职责、改动边界、风险点和文档要求

这几条如果真正执行下来，你这个项目的可维护性和可审查性会明显提升。
