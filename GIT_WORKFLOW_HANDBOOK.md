# Git / 分支 / Worktree / Codex App 使用总手册

## 1. 这份手册怎么用

这份手册是对当前目录下几份 Git 与工作流文档的总整理，目的不是替代细节文档，而是给你一个统一入口。

适合这样使用：

- 想快速建立整体认知时，先看这份
- 遇到具体问题时，再跳转到对应专题文档
- 后续把它当成项目级 Git 工作规范的总索引

关联文档：

- [GIT_BRANCH_WORKTREE_GUIDE.md](/E:/TODO/030%20+%20309/GIT_BRANCH_WORKTREE_GUIDE.md)
- [CODEX_APP_WORKTREE_PRACTICE.md](/E:/TODO/030%20+%20309/CODEX_APP_WORKTREE_PRACTICE.md)
- [PROJECT_GIT_WORKFLOW_STANDARD.md](/E:/TODO/030%20+%20309/PROJECT_GIT_WORKFLOW_STANDARD.md)

---

## 2. 你这个项目最该怎么理解 Git

对当前这个 STM32 / Keil 固件项目，最重要的不是记一堆命令，而是先把下面这几个概念分清：

- 提交 `commit`：历史快照
- 分支 `branch`：一条开发线
- 工作区 `working tree`：当前目录里真实正在修改的文件
- 暂存区 `staging area`：下一次提交准备带走的内容
- `worktree`：同一个仓库的额外工作目录

一句话记忆：

- 分支解决“历史怎么分线”
- `worktree` 解决“本地怎么并行开多个工作目录”

---

## 3. 对你当前项目最重要的结论

结合你现在的仓库状态和开发方式，最实用的结论是：

### 3.1 只做一个长期任务时

只开分支通常就够了。

例如：

- 单独做一次较大的协议修复
- 单独做一轮 map / RAM 优化
- 单独做一个模块重构

### 3.2 重构和维护并行时

分支加 `worktree` 最稳。

例如：

- 原目录继续修兼容问题
- 新目录专门做架构重构

### 3.3 当前目录已经改到一半，又来了新任务时

优先考虑新开 `worktree`，不要急着在同一个目录里 stash、切分支、再切回来。

### 3.4 大改动必须带文档

这在你的项目里应该是硬规则。

以下类型默认都算大改动：

- 通信层重构
- 协议适配层重写
- 存储 / 唤醒 / 休眠流程调整
- 驱动逻辑变化
- 兼容逻辑变化
- map / 内存布局优化

---

## 4. 推荐你的固定目录模式

建议后续长期固定为三类目录。

### 4.1 主目录

`E:\TODO\030 + 309`

职责：

- 当前维护线
- 小修小补
- 编译验证
- 兼容性修正

### 4.2 重构目录

`E:\TODO\030 + 309-refactor`

职责：

- 架构重构
- 模块拆分
- 分层改造
- 长周期整理

### 4.3 热修目录

`E:\TODO\030 + 309-hotfix`

职责：

- 紧急问题
- 最小修复
- 不打断重构和维护线

---

## 5. 推荐你的固定分支模式

建议统一使用可读、单一目标、带前缀的分支名。

推荐格式：

- `codex/refactor-architecture`
- `codex/hotfix-protocol-mapping`
- `codex/legacy-board-compat`
- `codex/cleanup-header-deps`

原则：

- 一个分支只承载一个目标
- 分支名直接说明任务
- 不用模糊名字

---

## 6. 推荐你的固定工作流

后续每次开工，建议都按这个顺序判断。

### 6.1 先定义任务类型

先判断这次是：

- 热修
- 小改动
- 中等整理
- 大重构

### 6.2 再决定隔离方式

选择规则：

- 只做一个任务：开分支即可
- 多任务并行：分支加 `worktree`
- 当前目录已经有未提交改动，又要开新任务：优先 `worktree`

### 6.3 再定义本次边界

先写清楚：

- 这次只改什么
- 这次不改什么
- 是否允许行为变化
- 是否要补文档

### 6.4 最后再动代码

这一步看似啰嗦，但对固件项目很值，因为它能明显减少“顺手改乱”的情况。

---

## 7. 在 Codex App 里怎么用

你在 Codex App 里可以按两种方式工作。

### 7.1 快速并行模式

直接让 Codex 在隔离副本中处理任务。

适合：

- 短期分析
- 临时热修
- 不想自己管目录

### 7.2 手动可控模式

自己先创建 Git `worktree`，再在 Codex App 中打开新目录。

适合：

- 长期重构
- 主线维护与重构并行
- 需要明确控制目录和分支

对你来说，更推荐第二种。

---

## 8. 当前项目的纪律要求

这是最值得长期坚持的部分。

### 8.1 一个目录只做一类事

不要在重构目录顺手做热修。  
不要在热修目录顺手做工程整理。

### 8.2 一个提交只表达一件事

不要把：

- 协议修复
- 工程清理
- 驱动调整
- 注释补充

揉成一个提交。

### 8.3 大改动必须配文档

不要等改完再想“要不要补”。  
开始重构时就应该建立文档。

### 8.4 提交前剔除环境噪音

像下面这些文件默认不应混进功能提交：

- `.uvoptx`
- `.uvguix.*`
- `.map`
- `.lst`
- `Objects/`
- `DebugConfig/`

如果确实要提交，必须说明原因。

---

## 9. 推荐给 Codex 的固定提示词

### 9.1 维护线

```text
你当前在主维护线工作，只处理现有版本上的问题，不做整体重构。
请先阅读相关模块，分析最小修改范围，再进行修复。
要求：
1. 全部使用中文输出
2. 优先最小改动
3. 涉及协议映射、寄存器、时序、内存布局或兼容逻辑时，必须说明风险
4. 如果改动较大，补充文档并整理清晰的 git 提交说明
```

### 9.2 重构线

```text
你当前在独立重构分支或 worktree 中工作，这里只处理架构重构，不处理主线上的临时修复。
请先阅读项目结构，识别通信层、协议层、驱动层、业务层之间的边界和耦合，再给出分阶段方案并逐步实施。
要求：
1. 全部使用中文输出
2. 非必要不改变现有功能行为
3. 大的代码变更必须配套文档
4. 每个阶段结束后整理清晰、可审阅的 git 提交说明
5. 涉及内存布局、协议映射、状态机或时序变化时必须明确说明风险
```

### 9.3 热修线

```text
你当前在独立 hotfix 分支或 worktree 中工作，这里只处理紧急修复，不做重构。
请先定位问题最相关的文件、调用链和影响范围，再按最小改动原则完成修复。
要求：
1. 全部使用中文输出
2. 不顺手整理无关代码
3. 保持现有行为稳定优先
4. 完成后给出验证建议和清晰的 git 提交说明
```

---

## 10. 你后续只需要记住这些

如果以后只想记最关键的几条，记住下面这些就够了：

- 分支是开发线，`worktree` 是额外工作目录
- 当前目录改到一半，又来了新任务时，优先考虑 `worktree`
- 只做一个任务时，开分支通常就够了
- 重构和维护并行时，分支加 `worktree` 最稳
- 一个目录只做一类事
- 一个提交只表达一件事
- 大改动必须配文档
- 提交前剔除环境和构建噪音文件

把这几条执行稳定了，你这个项目后续的 Git 使用就会清楚很多。
