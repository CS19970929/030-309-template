# 2026-03-17 瘦身记录

## 范围

本轮按要求先避开 `Comm` 通信模块，转而处理可安全修改的非通信 UTF-8 文件。

## 已完成

### `Code/Source/Sci_Upper.c`

- `Sci_DataInit()` 改为使用 `memset()` 清空 `u16Buffer` 和 `g_u8SCITxBuff`。
- 新增公共 helper：
  - `Sci_AppendU16()`
  - `Sci_AppendBytes()`
  - `Sci_GetDisplayCurrent()`
  - `Sci_FillDisplaySummary()`
- `Sci_ACK_0x03_ReadRegs_LCD()` 中：
  - `case 0` 改为复用 `Sci_FillDisplaySummary()`
  - `case 2` 改为复用 `Sci_AppendBytes()`

## 目的

- 压缩 `sci_upper.o` 的重复回包拼装代码。
- 不改寄存器地址、字段顺序、协议格式和业务判定，只优化实现方式。

## 后续建议

- `Sci_ACK_0x03_ReadRegs_LCD()` 的 `case 3` 仍有和 `case 0` 高度重复的摘要拼包逻辑，可以继续合并。
- `Sci_ACK_0x03_ReadRegs_Data()` 是 `sci_upper.o` 里更大的热点，但建议先按字段组拆 helper，再做下一轮压缩。
- `SH367309_Func.c`、`SH367309_DataDeal.c`、`SocEnhance.c` 仍然是整机 `map` 大头，但这些文件当前不是 UTF-8，建议先统一编码后再做函数级重构。

## HardFault 排查

这轮额外处理了一个高风险稳定性问题。

现象依据：

- 你提供的调试截图里，`PC=0x08006C02`，实际已经落在 `HardFault_Handler()`。
- 调用栈上一层落在 `App_PubOPUPChk()`。
- 同时寄存器 `R0=0x20000001`，作为 `SPUBOPUPCHK *` 入参时是明显异常的奇地址。

结论：

- 这类现象更像“上游 RAM 被踩坏后，`App_PubOPUPChk()` 收到坏指针”。
- 当前最可疑根因在调度器 `SCH_Add_Task()`：
  - 原代码先访问 `SCH_task_G[index]`，再判断 `index < SCH_MAX_TASKS`
  - 当任务表已满时，会先越界访问一次任务数组
  - 这种越界非常容易破坏后续栈或全局变量，最终在任意业务函数里触发 `HardFault`

本轮已修复：

- `Code/Source/BSP/Time_Triggered.c`
  - 修正 `SCH_Add_Task()` 的边界判断顺序，先判 `index < SCH_MAX_TASKS`
  - `SCH_Task_Delete()` 增加索引越界保护
  - `SCH_Dispatch_Tasks()` 增加 `pTask != 0` 保护，避免空函数指针调用
- `Code/Source/PubFunc.c`
  - `App_PubOPUPChk()` 增加最小化保护：
    - 奇地址指针直接返回
    - 空指针或空计数器指针直接返回

说明：

- `App_PubOPUPChk()` 的保护是“止血”，避免再次因为坏指针直接炸机。
- 真正更像根因的是调度器数组越界，这个问题优先级更高。
